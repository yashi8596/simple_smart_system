<div class="container mx-auto">

  <div class="row my-3 mx-auto">
    <h4 class="col-12 text-center mx-auto">アカウント削除確認画面</h4>
  </div>

  <div class="row py-3 col-11 mx-auto border border-dark rounded">
    <table class="table table-borderless">

      <tr>
        <th class="ml-auto col-5">従業員番号</th>
        <td class="mr-auto col-5"><%= @employee.employee_number %></td>
      </tr>
      <tr>
        <th class="ml-auto col-5">従業員名</th>
        <td class="mx-auto row">
          <%= @employee.last_name %> <%= @employee.first_name %>
        </td>
      </tr>
      <tr>
        <th class="ml-auto col-5">従業員名(カナ)</th>
        <td class="mx-auto row">
          <%= @employee.last_name_kana %> <%= @employee.first_name_kana %>
        </td>
      </tr>
    </table>

    <%= form_with model: @employee, url: admin_employee_path, method: :delete do |f| %>

      <div class="row py-3 col-11 mx-auto">
        <table class="table table-borderless">
          <tr>
            <th class="ml-auto"><%= f.label "管理者パスワード" %></th>
            <td class="mr-auto"><%= f.text_field :admin_password, class:"form-control" %></td>
          </tr>
        </table>

        <div class="row py-2 col-11 mx-auto border border-dark rounded">
          <p class="py-2 col-10 m-auto text-center">
            退職される従業員が上記の者で
            間違いがないことを確認しました。
          </p>
          <%= f.check_box :confirm, class:"form-control mx-auto col-2 my-auto" %>
        </div>


        <div class="row py-2 col-11 mx-auto">
          <%= f.submit "アカウントを削除する", class: "btn btn-danger col-5 mx-auto" %>
        </div>

      </div>
    <% end %>
  </div>
</div>