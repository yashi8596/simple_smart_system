<div class="container mx-auto">

  <div class="row my-3 mx-auto">
    <h4 class="col-4 text-center mx-auto">承認済有給申請詳細画面</h4>
  </div>

  <div class="row py-3 col-11 mx-auto border border-dark rounded">
    <table class="table table-borderless">

      <tr>
        <th class="ml-auto">従業員番号</th>
        <td class="mx-auto"><%= @leave_request.employee.employee_number %></td>
      </tr>
      <tr>
        <th class="ml-auto">従業員氏名</th>
        <td class="mx-auto row">
          <%= @leave_request.employee.last_name %> <%= @leave_request.employee.first_name %>
        </td>
      </tr>
        <tr>
          <th class="ml-auto">取得希望日</th>
          <td class="mx-auto"><%= @leave_request.preferred_date %></td>
        </tr>
        <tr>
          <th class="ml-auto">申請理由</th>
          <td class="mx-auto"><%= @leave_request.reason_for_request %></td>
        </tr>
        <tr>
          <th class="ml-auto">申請ステータス</th>
          <td class="mx-auto text-success">
            <% if @leave_request.permitted? %>
              <%= "承認されています。" %>
            <% end %>
          </td>
        </tr>

    </table>

    <div class="row py-2 col-11 mx-auto">
      <%= link_to "#{@leave_request.employee.last_name}#{@leave_request.employee.first_name}さんの従業員詳細画面を見る", admin_employee_path(@leave_request.employee.employee_number), class:"col-5 btn btn-info text-white mx-auto" %>
    </div>
  </div>
</div>